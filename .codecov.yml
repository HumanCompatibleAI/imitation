coverage:
  status:
    project:
      default: false
      main:
        target: 80%
        paths:
          - "imitation/"
          - "!imitation/examples/"
          - "!imitation/scripts/"
      auxiliary:
        target: 0%
        paths:
          - "imitation/examples/"
          - "imitation/scripts/"
    patch:
      default: false
      main:
        paths:
          - "imitation/"
          - "!imitation/examples/"
          - "!imitation/scripts/"
      auxiliary:
        paths:
          - "imitation/examples/"
          - "imitation/scripts/"

fixes:
  # Coverage report must match Git directory structure
  # Since we restrict coverage to just installed package, map back here.
  - "::imitation/"