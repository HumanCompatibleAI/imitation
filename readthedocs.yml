version: 2

sphinx:
  configuration: docs/conf.py

formats: all

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"
  jobs:
    post_create_environment:
      - pip install --upgrade setuptools==66.1.1

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
